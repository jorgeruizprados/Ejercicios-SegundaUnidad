function prime(){
   let variables1 =  new array ("telas","bies","hilo","tijeras","maquina de coser","boton")
variables1.splice(2,1);
variables1.splice(4,0,"hilo torzal")
variables1.splice(4,1,"mesa de corte","cuter rotatorio")
let variables2 = variables1.slice(0,2);
let variables3 = variables1.slice(3,5)

variables2.splice(1,0,"remalladora");
variables3.splice(4,0,"cinta métrica");

console.log(variables2);
console.log(variables3);
console.log(variables1);

variables1.sort
let arrayNumeros = ["uno", "dos", "Tres", "cuatro", "Cinco"];
 arrayNumeros.sort(function (elem1, elem2) {
 if (elem1.toLocaleLowerCase() > elem2.toLocaleLowerCase()) {
 return 1;
 }
 if (elem1.toLocaleLowerCase() < elem2.toLocaleLowerCase()) {
 return -1;
 }
 return 0;
 });


 
}
for (let index = 0; index < array.length; index++) {
    let numero = prompt("introduce valores numericos")

   array[index] =
    
}
